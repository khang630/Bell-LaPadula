This program has a makefile. To compile the program, type "make".
To run the program, type "./project1 input.txt", where input.txt is the input text file.
This program only takes 1 argument, i.e. the input file, and will ignore any additional CLI arguments.

Type "make clean" to clear object files and reset.